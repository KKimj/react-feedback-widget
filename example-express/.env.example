# ── 통합 e2e 서버(mini-e2e-server.mjs) 설정 ──
# 복사해서 .env 로 쓰거나 export 로 주입하세요.

# 서비스 계정 키 파일 경로 (Google Sheets + Cloud Storage 인증)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Firebase/Cloud Storage 버킷 (스크린샷 업로드 대상)
STORAGE_BUCKET=your-project.firebasestorage.app
# 버킷 내 업로드 경로 (공개 접근 정책은 rules 로 관리)
STORAGE_PATH=public/qa

# 구글 스프레드시트 ID (URL 의 /d/<ID>/edit)
GOOGLE_SPREADSHEET_ID=your-spreadsheet-id
# 기입할 시트 탭 이름
SHEET_NAME=Feedback

# Slack 알림 — Incoming Webhook (간단)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx/yyy/zzz
# 또는 Bot token 모드 (thread 답글 등 고급 기능용)
# SLACK_BOT_TOKEN=xoxb-...
# SLACK_CHANNEL=#feedback

# 서버 포트
PORT=3010

# ── feedback.config.js (destination / UI) ──
# 위젯 모달 레이아웃: centered | drawer | compact | stepper | two-column
FEEDBACK_UI_VARIANT=two-column
# Slack 카드에 표시할 채널명 (실제 전송 채널은 webhook 이 결정)
SLACK_CHANNEL=#feedback
# destination on/off (기본 활성, "false" 로 끄기)
ENABLE_SHEETS=true
ENABLE_SLACK=true
